@using Microsoft.AspNetCore.Identity
@using Microsoft.AspNetCore.Identity
@inject SignInManager<IdentityUser> SignInManager
@inject UserManager<IdentityUser> UserManager

<ul class="navbar-nav align-items-lg-center">
    @if (SignInManager.IsSignedIn(User))
    {
        var currentUser = await UserManager.GetUserAsync(User);
        var roles = currentUser == null ? Array.Empty<string>() : await UserManager.GetRolesAsync(currentUser);
        var roleName = roles.Count > 0 ? roles[0] : "Customer";
        var roleText = roleName switch
        {
            "Admin" => "管理员",
            "Warehouse" => "仓库员",
            "Support" => "客服",
            "Customer" => "客户",
            _ => "客户"
        };

        <li class="nav-item me-2">
            <span class="badge bg-info text-dark">@roleText</span>
        </li>
        <li class="nav-item me-2">
            <a class="nav-link" asp-controller="Profile" asp-action="Index">个人中心</a>
        </li>
        <li class="nav-item">
            <span class="nav-link">您好，@UserManager.GetUserName(User)</span>
        </li>
        <li class="nav-item">
            <form class="form-inline" asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Content("~/")" method="post">
                <button type="submit" class="nav-link btn btn-link">退出</button>
            </form>
        </li>
    }
    else
    {
        <li class="nav-item">
            <a class="nav-link" asp-area="Identity" asp-page="/Account/Login">登录</a>
        </li>
        <li class="nav-item">
            <a class="nav-link" asp-area="Identity" asp-page="/Account/Register">注册</a>
        </li>
    }
</ul>
